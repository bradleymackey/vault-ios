# These jobs are run on everything, including main and PRs.
name: Validate

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

env:
  IOS_PLATFORM: iOS Simulator
  IOS_DEVICE: iPhone 17 Pro
  IOS_VERSION: "26.0"
  XCODE_VERSION: "26.0"

jobs:
  lint:
    name: Lint
    timeout-minutes: 2
    runs-on: macos-26
    defaults:
      run:
        working-directory: ./Vault

    steps:
      - uses: actions/checkout@v4
      - run: sudo xcode-select -s /Applications/Xcode_${{ env.XCODE_VERSION }}.app/Contents/Developer
      - name: Check Linting & Formatting
        run: make lint

  build:
    name: Build
    timeout-minutes: 5
    runs-on: macos-26

    steps:
      - uses: actions/checkout@v4
      - run: sudo xcode-select -s /Applications/Xcode_${{ env.XCODE_VERSION }}.app/Contents/Developer
      - name: Build CI_iOS
        run: xcodebuild -workspace Vault.xcworkspace -scheme CI_iOS -destination 'platform=${{ env.IOS_PLATFORM }},name=${{ env.IOS_DEVICE }},OS=${{ env.IOS_VERSION }}' -skipMacroValidation -skipPackagePluginValidation build

  # test:
  #   name: Test
  #   timeout-minutes: 30
  #   runs-on: ubuntu-latest
  #   # runs-on: macos-14
  #
  #   steps:
  #     # We currently don't run tests in CI due to cost.
  #     # This can be re-enabled when the repo is made public!
  #     - name: Skip Testing
  #       run: true
  #     - uses: actions/checkout@v4
  #      - run: sudo xcode-select -s /Applications/Xcode_15.3.app/Contents/Developer
  #      - name: Run all Tests
  #        run: xcodebuild -workspace Vault.xcworkspace -scheme CI_iOS -destination 'platform=${{ env.IOS_PLATFORM }},name=${{ env.IOS_DEVICE }},OS=${{ env.IOS_VERSION }}' -skipMacroValidation -quiet test
